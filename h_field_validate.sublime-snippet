<snippet>
  <tabTrigger>h_field_validate</tabTrigger>
  <content><![CDATA[/**
 * Implements hook_field_validate().
 */
function ${TM_FILENAME/([^.]*)\..*$/$1/}_field_validate(\$entity_type, \$entity, \$field, \$instance, \$langcode, \$items, &\$errors) {
  foreach (\$items as \$delta => \$item) {
    if (!empty(\$item['value'])) {
      ${1:/* Your code here */}
    }
  }
}
]]></content>
</snippet>